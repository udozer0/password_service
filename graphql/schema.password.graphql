type GeneratedPassword {
  value: String!
  length: Int!
  strength: String!
}

input GeneratePasswordInput {
  length: Int!
  useDigits: Boolean!
  useSymbols: Boolean!
}

type Query {
  ping: String!
}

type Mutation {
  generatePassword(input: GeneratePasswordInput!): GeneratedPassword!
}
